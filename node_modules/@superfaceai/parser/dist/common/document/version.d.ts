/**
 * Structure representing abstract semver version tag.
 */
export declare class VersionRange {
    readonly major: number;
    readonly minor?: number | undefined;
    /** Patch version cannot appear without minor version. */
    readonly patch?: number | undefined;
    /** Label can appear even without major and minor version. */
    readonly label?: string | undefined;
    static fromString(input: string): VersionRange;
    static fromParameters(params: {
        major: number;
        minor?: number;
        patch?: number;
        label?: string;
    }): VersionRange;
    toString(): string;
    private constructor();
}
