import { ProfileId } from '.';
import { VersionRange } from './version';
/**
 * Class representing map version, every property except patch and label is required
 * Difference between this class and VersionRange is in optionality of properties - VersionRange is a more abstract structure
 */
export declare class MapVersion {
    readonly major: number;
    readonly minor: number;
    readonly revision?: number | undefined;
    static fromVersionRange(input: VersionRange): MapVersion;
    static fromString(input: string): MapVersion;
    static fromParameters(params: {
        major: number;
        minor: number;
        revision?: number;
    }): MapVersion;
    toString(): string;
    private constructor();
}
/**
 * Represents default value of profile version in ProfileId instance
 */
export declare const DEFAULT_MAP_VERSION: MapVersion;
/**
 * Class representing map id
 */
export declare class MapId {
    readonly profile: ProfileId;
    readonly version: MapVersion;
    readonly provider: string;
    readonly variant?: string;
    static fromParameters(params: {
        profile: ProfileId;
        version: MapVersion;
        provider: string;
        variant?: string;
    }): MapId;
    private constructor();
    toString(): string;
}
