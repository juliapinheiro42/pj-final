import { JessieSyntaxProtoError } from './transpiler/transpiler';
import { ForbiddenConstructProtoError } from './validator/validator';
/**
 * Validates and transpiles Jessie script.
 *
 * This functions combines the transpiler and validator in a more efficient way
 */
export declare function validateAndTranspile(input: string): {
    kind: 'success';
    output: string;
    sourceMap: string;
} | {
    kind: 'failure';
    errors: (JessieSyntaxProtoError | ForbiddenConstructProtoError)[];
};
