import { MapDocumentNode, ProfileDocumentNode } from '@superfaceai/ast';
export declare class Parser {
    private static mapCache;
    private static profileCache;
    static parseMap(input: string, fileName: string, info: {
        profileName: string;
        providerName: string;
        scope?: string;
    }): Promise<MapDocumentNode>;
    static parseProfile(input: string, fileName: string, info: {
        profileName: string;
        scope?: string;
    }): Promise<ProfileDocumentNode>;
    static clearCache(): Promise<void>;
    private static loadCached;
    private static clearFileCache;
    private static writeFileCache;
}
